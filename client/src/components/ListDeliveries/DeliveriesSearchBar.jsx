import { Grid, TextField } from '@mui/material';
import React from 'react';

const DeliveriesSearchBar = () => {
  return <Grid container xs={10}>
    <Grid></Grid>
  </Grid>;
};

export default DeliveriesSearchBar;
